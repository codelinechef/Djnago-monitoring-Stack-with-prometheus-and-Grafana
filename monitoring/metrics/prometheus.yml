global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'django'
    metrics_path: /metrics  # ✅ must match your Django route
    static_configs:
      - targets: ['web:8000']  # ✅ must match docker-compose service name and port
